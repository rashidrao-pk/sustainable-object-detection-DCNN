# Push to GitHub

```bash
cd /path/to/sustainable-dcnn-feature-fusion-clean
git init
git add .
git commit -m "Restructure repository and improve documentation"
git branch -M main
git remote add origin https://github.com/<your-username>/<your-repo-name>.git
git push -u origin main
```
